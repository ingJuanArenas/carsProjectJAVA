services:
  - type: web
    name: cars-backend
    runtime: java
    buildCommand: "./mvnw clean package -DskipTests"
    startCommand: "java -jar target/cars-1.0.0.jar"
    envVars:
      - key: DATABASE_URL
        sync: false
